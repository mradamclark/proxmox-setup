# Cloudflare API token scoped to Zone:DNS:Edit on the zone you're issuing
# certs for. Create at https://dash.cloudflare.com/profile/api-tokens.
# This is the token Traefik uses to write the _acme-challenge TXT record
# during the DNS-01 challenge.
CF_DNS_API_TOKEN=your-cloudflare-token-here
